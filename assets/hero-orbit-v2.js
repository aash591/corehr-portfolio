(function(){'use strict';var desk=document.querySelector('.hero-desk'),cards=[].slice.call(document.querySelectorAll('[data-orbit-card]'));if(!desk||!cards.length)return;var reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches,tau=Math.PI*2,focus=-Math.PI/2,start=performance.now();function dist(a,b){return Math.abs((a-b+Math.PI)%tau-Math.PI)}function focusAmount(d){var r=.47;if(d>=r)return 0;var x=1-d/r;return x*x*(3-2*x)}function frame(now){var rect=desk.getBoundingClientRect(),mobile=innerWidth<=640,small=innerWidth<=390,rx=mobile?Math.min(rect.width*.355,142):Math.min(rect.width*.39,208),ry=small?160:(mobile?178:194),elapsed=(now-start)/1000,base=reduced?0:(elapsed/42)*tau;cards.forEach(function(card,i){var a=base+(i/cards.length)*tau+focus,d=dist(a,focus),f=reduced?0:focusAmount(d),jig=reduced?0:Math.sin(elapsed*1.35+i*1.7)*3.2*(1-f*.72),radial=reduced?0:Math.sin(elapsed*.92+i*2.1)*2.5*(1-f),x=Math.cos(a)*(rx+radial),y=Math.sin(a)*(ry+radial),scale=1+f*(mobile?.17:.22);card.style.transform='translate(-50%,-50%) translate('+x.toFixed(1)+'px,'+y.toFixed(1)+'px) rotate('+jig.toFixed(2)+'deg) scale('+scale.toFixed(3)+')';card.style.zIndex=String(3+Math.round(f*7));card.classList.toggle('is-focus',f>.52)});if(!reduced)requestAnimationFrame(frame)}requestAnimationFrame(frame)})();